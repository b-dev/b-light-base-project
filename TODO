Vagrant provision doing stuffed based on .env values (like skipping upgrade)
requirements for db based on the value of PRJ_ENGINE
generate correct locale based on machine locale (default) or command arg
add integration to jenkins
find a box with guest additions for 4.2
have the upgrading of debian packages on vagrant provisiong optional from .env variable
have the pip install/upgrade optionally from .env variable
configure port forwarding to reach postgres from the host
bootstrapping should clear the README, adding a reference to the base project as header
add DBBACKUP_TOKENS_FILEPATH, DBBACKUP_DROPBOX_APP_KEY, DBBACKUP_DROPBOX_APP_SECRET in .env file